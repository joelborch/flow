## What

<!-- One or two sentences: what does this PR change and why? -->

## Checklist

- [ ] `pnpm typecheck` and `pnpm -r test` pass locally
- [ ] No secrets or real config values (account IDs, hostnames, tokens, `wrangler.jsonc`) are included — only `wrangler.example.jsonc` placeholders
- [ ] Changes to the API contract (routes, `@flow/shared` schemas, DO storage shape, MCP tools) are called out below
- [ ] Docs / READMEs updated if behavior or setup changed

## Contract changes

<!-- If this touches the API surface, shared zod schemas, Durable Object storage,
     or wrangler config shape, describe the migration/compat story. Otherwise write "None". -->

None
